// .sequelizerc

const path = require("path");

module.exports = {
  config: path.resolve("storage/config", "config.json"),
  "models-path": path.resolve("storage", "models"),
  "seeders-path": path.resolve("storage", "seeders"),
  "migrations-path": path.resolve("storage", "migrations"),
};
